spring:
  datasource:
    url: jdbc:mysql://localhost:3306/newdb
    username: root
    password: admin
  jpa:
    database: mysql
    show_sql: true
    hibernate:
      ddl-auto: create-drop
  jackson:
    serialization:
      FAIL_ON_EMPTY_BEANS: 'false'
